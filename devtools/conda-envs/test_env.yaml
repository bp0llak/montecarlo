# name: test
# channels:

#   - defaults
# dependencies:
#     # Base depends
#   - python
#   - pip

#     # Testing
#   - pytest
#   - pytest-cov
#   - codecov
#   - numpy

#     # Pip-only installs
#   #- pip:
#   #  - codecov

name: test
channels:
  - conda-forge
dependencies:
  # Base depends
  - python
  - pip
  - numpy

  # Testing
  - pytest
  - pytest-cov
  - codecov
  - numpy

  # Pip-only installs
  - pip:
      - .[test]